# int / enum 真实 20 轮测试

- 服务：DeepSeek，模型 `deepseek-chat`，端点 `https://api.deepseek.com/chat/completions`。
- 实际调用 20 次，HTTP 均为 200；没有自动重试。
- 19/20 轮完整通过，95%。第 5 轮返回 39 个空格，缺少有效回复与状态补丁，本地解析器拒绝提交；第 6～20 轮正常继续。
- 总计 48,981 tokens，20 次请求延迟合计约 29.55 秒。
- 通过轮次的整数加减、零值、枚举、空值保留以及当前状态均符合预期，固定 system 前缀在整个测试中保持一致。
- 最终状态：零钱 102、线索数 5、时间“深夜”、心情“平静”、地点为空。

第 5 轮用户输入包含纸上的虚构指令，明确声明没有实际状态变化。API 返回的是空格，无法据此判断供应商为何生成空响应。此结果属于真实失败，没有重新请求并用成功结果覆盖。应用对无效响应的处理符合设计：不提交状态，用户可以手动重试。

本评测使用真实网络请求、项目提示词组织及 RoleplayTurn/reducer 校验，覆盖一个连续 20 轮的确定性场景；不代表所有模型、剧情、手机或网络条件的通过率。本轮另有 100 个离线核心测试全部通过。

[逐轮输入、原始响应、状态与用量](typed_state_live_2026-09-09.json)。密钥只从进程环境读取，未保存到报告。
