# 默认 100 个核心测试

默认命令 `flutter test --no-pub` 精简为 100 个测试，移除重复的默认值、简单 getter、类似协议变体及已被事务测试覆盖的用例。原有测试可从 Git 历史找回；没有用 skip、名称过滤或隐藏测试目录凑数。手机创建窗口的七种尺寸、三档字体归为一个布局矩阵用例，仍执行全部组合。

| 覆盖范围 | 测试数 |
| --- | ---: |
| SQLite 剧情回滚、分支、重启及损坏备份 | 18 |
| 对话事务、取消、流式失败、备用 API | 12 |
| 状态类型、兼容导入、enum/int 编辑器 | 10 |
| JSON 字段、全半角、错误字符诊断 | 8 |
| 提示词分层与真实历史 | 4 |
| HTTP 请求、SSE、错误及召回预算 | 6 |
| 事件召回、候选边界及取消 | 6 |
| 记忆增删改与物品维护 | 4 |
| 备份格式与往返 | 2 |
| API 配置保存 | 3 |
| JPG 头像与图片 API | 3 |
| TTS 请求及错误 | 3 |
| 手机资料、API 输入、创建布局及导入修正 | 16 |
| 旧数据迁移 | 1 |
| 消息分页 | 3 |
| 启动与加载 | 1 |
| 合计 | 100 |

真实 API 测试单独运行，不计入默认 100 个，也不会在普通测试时自动消费 Key：

```powershell
# DEEPSEEK_API_KEY 仅设置在当前进程环境中，不写入文件。
flutter test tool/typed_state_live_eval_test.dart --no-pub --reporter expanded
```

该工具固定调用 DeepSeek / deepseek-chat，共 20 轮，复用应用实际提示词组织和 RoleplayTurn 状态校验。覆盖整数加减/零值、枚举切换、状态保留与显式清空。不会自动重试、伪造模型输出或修正失败结果；报告保留逐轮原始响应、token 用量和状态对照，不保存 Key 或请求头。未提供 Key 会明确失败，不会静默跳过。

[本次真实测试结果](../reports/typed_state_live_2026-09-09.md)。
